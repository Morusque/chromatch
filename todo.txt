
to review :

next tasks :
- "show matches only" should also show the track tracks are matching with as well as tracks currently loaded in the display zone

tasks for later :
- some tracks (eg "02 - Le Chien...") are still cropped when loaded to the display (might be due to the file itself though)
- zoomed waveform resolution could be even better probably
- activating beat sync could immediatly resync everything even already playing
- sometimes tracks that are beat synced (but started in specific ways) have their vertical bars not align, which isn't supposed to happen
- playback is better now, still has some occasional glitch when another track starts while some already play, or while loading another track
- tempo gap filter like with the old tool (limit tempo differences when searching for similar tracks (or weight it, to be tested later))
- actually loop loops if some per-track loop checkbox enabled
- there should be a way to quantize cues and loops to closest beat or not (maybe checkbox, on by default)
- find a way for the parts to be more linked together (eg just one line with something like "1/2" in the Part column and a "next part" button somewhere)
- figure out how "base" could (or could not) be calculated and aim for an automatic base detection
- dropping a track takes some processing time, why?
- mixing tools (eg DJ-style filter)
- tempo glide when auto-changing
- make graphviz graph better
- export as traktor/mixxx file
- automix

check this :
- manually track and reencode files not parsed correctly

maybe at some point :
- option to relink track with file
- option to keep absolute and/or relative url of files
- detect when bars and audio get out of sync and resync them over the course of the track
- handle case where tempo is tapped over a modified tempo (because of tempo or multiplier) check if this really works, not sure
- analysis and variable to state if a track is strongly correlated with its tempo (ie easily "mixable") or not
- auto beat detection could still be better, maybe refine this kind of detection on proven files
- handle time signatures (detect/redefine by user, same logic than tempo and downbeat)
- detect cue points for structure parts and loops
- handle variations in tempo and chroma (throughout a track)
- inverse associate with eq profile

