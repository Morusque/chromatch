
to review :

todo soon :

- zoomed :
  - (probably to be done later) allow to manually place two or more beats to help with tempo detection
- UI :
- detection :
  - handle variations in tempo and chroma (throughout a track)
  - handle time signatures (detect/redefine by user, same logic than tempo and downbeat)
  - detect/redefine cues for structure parts and loops

todo at some point :

playback :
- playback is better now, still has some occasional glitch when another track starts while some already play

need the other tool inspection first :
- integrate track infos from the former tool
- add the "match/supermatch" marking logic
- export Graphviz graphs
- separate the concept of "base" and chroma, allow for defining/detecting both of them

even later :
- export as traktor/mixxx file
- inverse match with eq profile
- mixing tools (eg filters)
- automix
- limit tempo differences when searching for similar tracks or weight it
- some tracks (eg "02 - Le Chien...") are still cropped when loaded to the display (might be due to the file itself though)
