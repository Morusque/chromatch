
done, to review :

todo soon :

zoomed :
- the zoomed in view could have a better resolution (maybe threaded or cached or something if that's too much to compute)
- in zoomed view beat bars and waveform could move more in sync with each other when shifted, zoomed etc

sync :
- have a master metronome (with a checkbox to hear it), sync beats to it
- option (checkbox) beat sync to metronome when pressin play on a track

UI :
- indicate current target(s) in list somehow, in a different way than selection

detection :
- why sorting by chroma/tempo often leads to similar tempi first top of the list? that sounds wrong but I'm not sure why, investigate it
- handle variations in tempo and chroma
- handle time signatures
- define cues for structure parts and loops

todo at some point :

playback :
- playback is better now, still has some occasional glitch when another track starts while some already play
- allow loops

need the other tool inspection first :
- integrate track infos from the former tool
- add the "match/supermatch" marking logic
- export Graphviz graphs

even later :
- export as traktor/mixxx file
- inverse match with eq profile
- mixing tools (eg filters)
- automix
- limit tempo differences when searching for similar tracks or weight it
