
to review :

next tasks :
- changing the fft window size when doing chromagram seems to result in drastically different outcomes, find out why and what's best
- detect cue points for structure parts and loops (eg a group of a given number of bars that loops nicely, a sudden change in gain density or harmony, etc)
- automix (many things to define about this)
- still occasional playback glitches
- inverse associate with eq profile (define a whole eq profile (not cycling like chroma) broadly, not very detailled) so we can check tracks that compensate each other
- handle variations in tempo and chroma (throughout a track)

tried already but could be better :
- auto beat detection could still be better, maybe refine this kind of detection on proven files
- beat anchor still completely off-phase for some tracks (eg Black Tears) despite several fixes this session: robust window filtering, transient-based detection, t=0 artifact fix, advance-to-first-transient. Root cause unclear — the phase the algorithm lands on seems genuinely wrong, not just a same-phase-different-beat display issue. Benchmark (tools/tempo_anchor_benchmark.py) can help reproduce.
- handle case where tempo is tapped over a modified tempo (because of tempo or multiplier) check if this really works, not sure
- revise graphviz graphs further
- investigate automatic base detection, if this is ever possible → investigated peak + Krumhansl-Schmuckler: K-S gives 19.4% exact vs 21.2% for plain peak (458 tracks). Many worst errors are tritone-apart because the dominant chroma note genuinely IS the tritone of the user's base (eg K298.mp3 in D has G# as top note). Root cause: chromatch "base" is an editorial mix-point concept, not the global statistical key — so whole-track histogram methods cannot work reliably. Would need bass-register-only analysis or temporal/cadential data.
- revise how the "mixable" score works

check this :
- decide if Mixxx export is still needed after Traktor NML, and which Mixxx import target should be used if yes
- decide what automix should optimize first: compatible sequence, live crossfade behavior, or offline playlist generation (TODO answer this)

work to do with files manually :
- some audio files still need re-encoding or something

maybe at some point :
- handle time signatures (detect/redefine by user, same logic than tempo and downbeat)
