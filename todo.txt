
to review :

next tasks :
- option to keep absolute and/or relative url of files
- option to relink track with file
- a way to manually change base frequency (as a directly editable field in the "tap tempo, reset tap, etc" lane)
- option to say "undefined tempo" when the track has none
- buttons to nudge tempo up or down very slightly (parametrable)
- tapping tempo is sometimes just plain wrong (eg detects about twice the tapped tempo)
- inverse associate with eq profile (define a whole eq profile (not cycling like chroma) broadly, not very detailled) so we can check tracks that compensate each other
- reorganize the UI in sections that make more sense
- still occasional playback glitches
- analysis and variable to state if a track is strongly correlated with its tempo or not and has stable chroma (ie easily "mixable") define a score for this
- handle variations in tempo and chroma (throughout a track)
- detect cue points for structure parts and loops
- automix (many things to define about this)

tried already but could be better :
- auto beat detection could still be better, maybe refine this kind of detection on proven files
- handle case where tempo is tapped over a modified tempo (because of tempo or multiplier) check if this really works, not sure
- revise graphviz graphs further
- investigate automatic base detection, if this is ever possible

check this :
- decide if Mixxx export is still needed after Traktor NML, and which Mixxx import target should be used if yes
- decide what automix should optimize first: compatible sequence, live crossfade behavior, or offline playlist generation (TODO answer this)

work to do with files manually :
- some audio files still need re-encoding or something

maybe at some point :
- handle time signatures (detect/redefine by user, same logic than tempo and downbeat)
